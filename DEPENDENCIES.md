Windows:
* *msvcrt*
Linux / MacOS:
* *pygame*  :   (sudo) dnf install python-pygame
                (sudo) apt install python3-pygame
                (sudo) dnf install tesseract        # OCR
* *tkinter* :   (sudo) dnf install python-tkinter
                (sudo) apt install python3-tk
